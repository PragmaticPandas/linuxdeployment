#!/bin/sh
#takes files from a directory that start with the phrase
#"append to" and adds them to the end of the file with
#the given name after the keywords in a different directory
#also searches directories recursively

#pseudocode:

#